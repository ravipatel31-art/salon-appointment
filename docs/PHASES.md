# Phase Status Board

Update **only rows you own**. Lead reconciles after each wave. States: `pending` | `in_progress` | `done` | `blocked`.

| Phase | Scope | Owner | State | Notes / blockers |
|---|---|---|---|---|
| 0a | Backend scaffold (FastAPI, docker-compose Postgres, Alembic, health) | salon-backend | done | Fixed `alembic/env.py` Path bug; compose port overridable via `SALON_DB_PORT`; `alembic upgrade head` verified on live Postgres 16 |
| 0b | Flutter scaffold (folders, routing, theme, dio client) | salon-flutter | done | Feature folders, go_router shell (tabs + pushed flows), Material theme, dio client + token store; `API_BASE_URL`/`USE_MOCK_API` via `--dart-define` |
| 1 | Masters + auth (JWT, services/barbers/schedules seed + admin CRUD) | salon-backend | done | JWT (PyJWT+bcrypt), AGENTS.md seed exact (8 services, wash addon, 5 barbers×7 schedules, admin from env), public catalog + admin CRUD/schedule; `python -m app.seed` idempotent on PG |
| 2 | Availability engine + booking create/hold/list/cancel | salon-backend | done | 15-min IST grid, half-open overlap, active-hold blocking; POST /bookings hold+advance via `payment_interface` → integration `payments.py`; verify/cancel ≥2h; webhook router wired in `main.py` |
| 3 | Razorpay order/verify/webhook/refund + hold expiry | salon-integration | done | `payments.py` + `api/webhooks.py` + `services/hold_expiry.py`; 40 tests green. Needs: backend `main.py` include of `webhooks.router`; CONTRACT_CHANGE proposed (`cancellation_reason`) |
| 4 | Admin bookings/dashboard APIs | salon-backend | done | list filters (date IST/barber/status), PATCH complete/no_show (color requires final → balance_due = final − online), dashboard aggregates; authz 403 covered by tests |
| 5 | Flutter UI (all screens + Razorpay SDK) | salon-flutter | done | All screens incl. 14-day picker, wash add-on live pricing, 12-min hold countdown, 409/retry paths, Razorpay SDK behind gateway interface (mock sheet default); `flutter analyze` clean, 45/45 tests green |
| 6 | Hardening (overlap tests, expiry job, refund path, e2e) | salon-integration | done | 15 new tests (`test_e2e_lifecycle/overlap/hold_expiry`, `test_payment_refund_path`, `real_payments` fixture): double-booking race, half-open overlap matrix, verify overlap re-check, hold TTL sweep (+`hold_expired` reason when column exists), refund→refunded + webhook backstop, full e2e register→…→dashboard; hold-expiry tests refreshed post-0003 (idempotent ALTER helper, "without column" case → post-delta API `hold_expired` assert); backend pytest 108/108 exit 0 |
| 7 | Deploy (Render/VPS + HTTPS, Play Store build notes) | salon-integration | done | Root `docker-compose.yml` (postgres+api, migrate→seed→uvicorn, healthchecks) + `deploy/Dockerfile.api` + `deploy/.env.example` + `deploy/NOTES.md` (VPS+Caddy HTTPS, Render, Razorpay webhook setup, Flutter `--dart-define`/Play Store, secrets checklist). Validated: `compose config` OK, full stack build+boot smoke test (migrations, seed, /health, /services, admin login, webhook 401) — no secrets in repo |
| 8 | Guest booking API (optional auth on POST /bookings, guest identity, guest token for verify/cancel/list) | salon-backend | done | Optional auth + `guest_name`/`guest_phone` → find-or-create **customer only** (admin phone → 400, never mints admin JWT); `is_guest` + nullable email migration `0004_guest_users`; guest token for verify/cancel/list; pytest **128 passed** incl. `test_guest_booking.py` |
| 9 | Barber-first UI + selection page + guest checkout (no login wall) | salon-flutter | done | Splash → `/barbers` (shell tab 0 = Barbers; Services secondary with `?service_id=` preselect); barber detail = selection page (service + wash + date + slot); guest name+phone on checkout without Bearer, stores `access_token` via `adoptSession`; forced login dialog removed. `flutter analyze` clean, **56/56** tests (mock-to-contract; live API ready after phase 8) |
| 10 | Test payment bypass (RAZORPAY_MOCK verify + Flutter PAYMENT_MOCK sheet) | salon-integration | done | Contract 2026-09-24: mock verify accepts non-empty triple when `RAZORPAY_MOCK=1`; Flutter `--dart-define=PAYMENT_MOCK=true` → MockPaymentGateway with real API. Backend **132 passed**; Flutter analyze clean + **61/61**. API image rebuilt with `RAZORPAY_MOCK=1`; local app run uses `USE_MOCK_API=false` + `PAYMENT_MOCK=true`. Note: integration also adjusted e2e bogus-signature case to whitespace-only (owned `test_e2e*`) so mock bypass does not auto-confirm twin booking |
| 11 | Seed barber `photo_url` demo portraits (HTTPS) | salon-backend | done | `BARBER_PHOTO_URLS` (i.pravatar.cc/300?u=salon-\<slug\>); null/empty back-filled on re-run, never overwrites; `test_seed_barber_photo_urls`; pytest **133 passed** |
| 12 | Visual polish — images + attractive customer UI | salon-flutter | done | Barber photos (`errorBuilder` → gradient + initials), `ServiceArt` icon/gradient map, richer cards/splash/empty states, mock `photo_url`s; widget-test fake portrait HttpClient. `flutter analyze` clean, **72/72** tests green |
| 13 | Admin browser dashboard (`/panel` — barbers + salon plan/services + bookings + dashboard) | salon-backend | done | Static SPA `backend/app/static/panel/` (index/app.css/app.js, vanilla, no CDN) served via `GET /panel` + SPA fallback in `main.py::_mount_panel`; email login → sessionStorage + admin-only 403 handling; tabs Barbers (CRUD + weekly schedule editor Mon=0…Sun=6), Services (plan CRUD, hair-colour variable_advance hint), Bookings (filters + complete w/ required final price for colour + no_show), Dashboard (IST date + aggregates). Brand wine/gold/cream. `tests/test_panel.py` ×6 (HTML/assets/fallback/traversal/`/docs` intact). Backend pytest **139 passed** |
| 14 | Panel full-page edit routes (`#/barbers/{id}/edit` etc.) + SQLi hardening tests/payloads | salon-backend | done | Hash router in `panel/app.js`: `#/barbers` `#/barbers/new` `#/barbers/{id}/edit`, `#/services*`, `#/bookings` `#/dashboard` roots, Back via `data-back`; barber fields + weekly schedule share one full-page editor (opens at top); delete/`final_price_at_center` stay modal. `/panel*` responses now send `X-Content-Type-Options: nosniff` + CSP (`default-src 'self'; frame-ancestors 'none'`, `img-src https:` for portraits); inline `onerror` removed (avatar fallback wired in JS). Audit: no string-built SQL in `app/` (ORM/bound params only). New `tests/test_admin_sqli.py` ×8 (POST/PATCH payloads in name/bio/description/photo_url, query-param probes, users/bookings/barbers/services row-count asserts, static SQL audit, panel `esc()`/no-eval) + `test_panel.py` +3 (headers, hash routes, `node --check`). Backend pytest **150 passed** (was 139). Note: phase-14 section not present in `docs/CONTRACT.md` when dispatched — implemented to the task scope |
| 15 | Panel path routes — separate `/panel/login` page → `/panel/dashboard` after auth | salon-backend | done | Path router in `panel/app.js` (`pushState`/`popstate`, hash router removed): `/panel` → client redirect login/dashboard (session is `sessionStorage`, server cannot choose), `/panel/login` = login page only (no tabs/shell), sign-in → `/panel/dashboard`, protected paths → `/panel/login` when logged out (401/403 too), logout → `/panel/login`, editors/tabs at `/panel/barbers*`, `/panel/services*`, `/panel/bookings`. Server unchanged — `/panel/login` + `/panel/dashboard` verified 200 HTML via SPA fallback with nosniff+CSP; assets still real files. CSS fix `[hidden]{display:none!important}` (author `display:flex` beat the UA hidden rule → shell used to render on the login page). `test_panel.py` +3 → pytest **153 passed**; headless-Chrome smoke (deep links, session restore, Back/popstate, logout, no CSP violations) all green. Contract delta 2026-09-24 |
| 16 | Panel app logo (login + topbar) | salon-backend | done | `panel/logo.svg` (hand-drawn wine/gold scissors roundel — launcher `ic_launcher.png` is default Flutter blue, off-brand) + `logo.png` copied from `mobile/…/mipmap-xxxhdpi/ic_launcher.png`; login shows 104px logo, topbar brand 34px, `<link rel="icon">` → same SVG; same-origin (CSP `img-src 'self'`). `test_panel.py` +3 → pytest **156 passed**; headless-Chrome screenshots of `/panel/login` + topbar verified |
| 17 | Docker one-shot stack + tunnel links + shop PDF | salon-integration / salon-lead | done | Stack complete (`db`+`api` includes panel/static) — `docker compose restart` / `up -d` → both healthy, `/panel/login`+logo 200. Tunnel: `cloudflared tunnel --url http://localhost:8000` then append `/panel/login`. Lead PDF: `docs/shop-guide.pdf` (4 pp: services, counter flow, admin panel, Docker + Cloudflare) from `docs/shop-guide.html` |
| 18 | GitHub repo + Actions workflow → iOS IPA | salon-integration | in_progress | Owner: push as **new** GitHub repo (no secrets); CI builds `flutter build ipa` on macOS runner with signing secrets documented. Workflow under `.github/workflows/` (integration-owned deploy/CI) |

## Wave log

| Wave | Date | Result |
|---|---|---|
| W1 | 2026-09-23 | 0a, 0b → `done` (backend scaffold + alembic fix; Flutter scaffold/routing/dio) — verified |
| W2 | 2026-09-23 | 1, 2, 3, 5 → `done` (auth/masters/seed; availability/bookings; Razorpay interface+webhook+hold-expiry; Flutter UI) — verified: backend pytest exit 0, flutter analyze+45/45 tests; CONTRACT_CHANGE applied (`cancellation_reason`) |
| W3 | 2026-09-23 | 4 → `done`; 6 → `done` (re-dispatched after 2 verified; 15 hardening/e2e tests, initially gated on phase 2) |
| W4 | 2026-09-23 | 7 → `done` (root compose + deploy/NOTES.md + Dockerfile.api, stack boot smoke tested). Follow-ups: contract delta `cancellation_reason` write-side shipped (backend migration 0003) + integration hold-expiry tests refreshed → suite **108/108 exit 0** |
| W5 | 2026-09-24 | 8, 9 → `done` (barber-first flow + guest checkout request). Contract delta: guest booking (customer-only find-or-create; admin phone rejected 400) + flow order. Backend pytest **128/128**, Flutter analyze clean + **56/56**. Follow-up: manual live guest pay against Postgres when deploying (mock already covered) |
| W6 | 2026-09-24 | 10 → `done` (test payment bypass). Backend pytest **132**, Flutter **61/61** + analyze clean. API container rebuilt `RAZORPAY_MOCK=1`; app relaunched on emulator with `USE_MOCK_API=false` + `PAYMENT_MOCK=true` for local guest booking without money |
| W7 | 2026-09-24 | 11, 12 → `done` (images + attractive UI). Contract: Imagery (barber `photo_url` HTTPS + fallback; service art client-side). Backend seed `photo_url` + **133** tests; Flutter visual polish (`BarberPhoto`/`ServiceArt`/brand) + analyze clean + **72/72**. Reseeded live DB; reinstalled app on emulator |
| W8 | 2026-09-24 | 13 → `done` (admin browser dashboard). Contract: Admin browser dashboard section applied. Static SPA `backend/app/static/panel/` at `GET /panel`; backend pytest **139**; API image rebuilt — live `http://127.0.0.1:8000/panel` returns 200 (Barbers/Services/Bookings/Dashboard) |
| W9 | 2026-09-24 | 14 → `done` (full-page edit routes + SQLi hardening). Contract: Panel edit routing + SQL injection hardening. Backend pytest **150**; API rebuilt — live `/panel` serves nosniff+CSP + `page-barber-edit`/`page-service-edit` |
| W10 | 2026-09-24 | 15 → `done` (separate `/panel/login` → `/panel/dashboard`). Contract: Panel path routes. Backend pytest **153**; API rebuilt — live 200 on `/panel/login`, `/panel/dashboard`, deep-link editors, nosniff+CSP |
| W11 | 2026-09-25 | 16, 17 → `done` (panel logo; Docker restart verified; Cloudflare tunnel link set; shop PDF). Logo on login+topbar (wine/gold SVG), pytest **156**; `docs/shop-guide.pdf` 4 pages |
| W12 | 2026-09-25 | Public API hostname wired: **`https://salonapp.unonomercysound.online`** → `/health` `/services` `/auth/login` `/panel/login` all 200. Contract Flutter base-URL note updated. Flutter relaunched on emulator with `API_BASE_URL=https://salonapp.unonomercysound.online` + `USE_MOCK_API=false` + `PAYMENT_MOCK=true` |
